
<!doctype html>
<html>
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Editează produs — Markt-shop</title><link rel="stylesheet" href="/style.css"></head>
<body>
  <main class="container admin">
    <% if (!product) { %><h2>Adaugă produs nou</h2><form method="post" action="/admin/add" class="form">
      <label>Nume<br><input name="name" required></label>
      <label>Descriere<br><textarea name="description"></textarea></label>
      <label>Preț (€)<br><input name="price" required></label>
      <label>Categorie<br><input name="category"></label>
      <label>Imagine (URL)<br><input name="image" placeholder="https://..."></label>
      <button class="btn" type="submit">Adaugă</button>
    </form><% } else { %>
      <h2>Editează: <%= product.name %></h2>
      <form method="post" action="/admin/edit/<%= product.id %>" class="form">
        <label>Nume<br><input name="name" value="<%= product.name %>" required></label>
        <label>Descriere<br><textarea name="description"><%= product.description %></textarea></label>
        <label>Preț (€)<br><input name="price" value="<%= (product.price_cents/100).toFixed(2) %>" required></label>
        <label>Categorie<br><input name="category" value="<%= product.category %>"></label>
        <label>Imagine (URL)<br><input name="image" value="<%= product.image %>"></label>
        <button class="btn" type="submit">Salvează</button>
      </form>
    <% } %>
    <p><a href="/admin/panel">Înapoi</a></p>
  </main>
</body>
</html>
